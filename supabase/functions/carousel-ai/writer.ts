// Stable carousel writer entry point. The transport also serves editorial review
// from _shared so edge bundles never import a neighbouring function directory.
export * from "../_shared/carousel-model.ts";
