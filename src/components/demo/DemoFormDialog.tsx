/**
 * @deprecated DemoFormDialog is no longer used.
 * Demo mode now uses static DEMO_DATA with a single-click activation.
 * Kept temporarily to avoid import errors during cleanup.
 */
export default function DemoFormDialog({ open, onOpenChange }: { open: boolean; onOpenChange: (open: boolean) => void }) {
  return null;
}
